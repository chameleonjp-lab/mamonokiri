# マージ後スマホ仕様フォローアップ

基準コード: `main` / `85fa757fbaa6eb44a438e6818ee5077f063fd139`  
作業ブランチ: `codex/post-merge-mobile-polish-20260823`  
対象端末: iPhone 17 Proを最優先。iPhone SE級と横画面も対象。

## 確認したこと

- PR #7で、スマホ入力、序盤3体の実技ゲート、Safari中断、Safe Area、画面レイアウト、描画停止、章報酬、Pages workflowはmainへ反映済み。
- 10・25・50体モード、難易度、章報酬、固定シード、同じ敵順での再挑戦、モード×難易度の自己ベスト保存もmainに存在する。
- 結果画面は自己ベストを保存しているが、保存済みの数値を通常結果で表示していなかった。
- 結果画面と一時停止画面の「最初から」は、現在の状態ではなくタイトルで最後に選んだ設定へ戻る余地があった。

## 原因

結果画面の表示が`isNewRecord`だけを見ており、更新がない場合の保存済み記録を表示していなかった。また、再挑戦の既定値がReact側のタイトル選択状態に依存していたため、イベント経由でモードや難易度が変わった場合に、現在のプレイ条件を保持できなかった。

## 変更内容

- 結果画面に、現在のモード・難易度に対応する自己最高スコアを常時表示。
- 結果画面と一時停止画面の「最初から」「もう一度遊ぶ」を、現在の`state.mode`と`state.difficulty`で再開する共通処理へ集約。
- 既存の「同じ敵順で再挑戦」のシード指定は維持。

## 影響範囲

戦闘判定、スコア計算、敵順、保存キー、4操作（左・右・斬・防）、音、描画処理は変更しない。結果表示と再挑戦時の設定引き継ぎだけを変更する。

## 検証

- 結果画面の自己ベスト表示が`bestScore`を参照することを静的確認。
- 通常再挑戦が現在のモード・難易度を明示して`yamabushi-restart`を送ることを静的確認。
- 既存のVitest、TypeScript、Prettier、Vite buildを実行する。

## 残課題

iPhone Safari実機で、結果カードの縦スクロール、Safe Area、回転直後の再挑戦、長時間プレイ時のフレーム落ちを確認する。実機確認なしにReady化・mainへのマージは行わない。
