# 優先度A 戦闘深化・敵差別化 実装記録

- 実施日: 2026-08-20
- 対象: `main` (`53f1fec365702b09e236c8c5d1c4a832d2ba84b1`) を起点とする `codex/priority-a-combat-depth-20260820`
- 方針: 操作ボタンは増やさず、斬・防御・左右回避のタイミングと敵予告の読み分けを増やす

## 1. 確認したこと

- 攻撃後の入力制限は飛刃の命中までしか実質的に働かず、防御・回避で隙を消せた。
- 受け流し受付は存在したが、プレイヤーと敵の構え値は存在しなかった。
- 敵防御中の斬は即時の防御崩しとなり、発生の遅さや失敗リスクがなかった。
- 通常敵7種は攻撃方向と数値差が中心で、固定型・追尾ロック・二段・重量の役割が未接続だった。
- ボス10種には5系統名と一部演出差があったが、章別出現、第二段階、系統固有の連続攻撃は未実装だった。
- 再挑戦ボタンは React のクリックイベントをゲームイベント名として渡し得る呼び出しになっていた。
- 終了済みタイマーもアプリ復帰時に延長され、古い受付時間が復活する可能性があった。

## 2. 原因

- 攻撃を単一の短い期限で管理し、振り始め・命中・振り終わりを区別していなかった。
- 防御結果を体力と受け流しの二択だけで処理し、有限資源となる構えを持っていなかった。
- 敵生成が全通常敵／全ボスの大域ランダムで、章の学習順と敵ごとの攻撃規則を持っていなかった。
- 一時停止復帰では「0より大きいタイマー」を一律に延長し、停止開始時点で有効だったかを判定していなかった。

## 3. 変更内容

### 戦闘

- 斬撃を `振り始め → 命中 → 振り終わり` の三段階に分けた。
- 攻撃中は斬・防御・回避を相互に重ねられないようにした。
- 通常斬撃、反撃斬り、防御崩し、大崩れへの強撃に異なる発生・隙・ダメージ・得点を設定した。
- 敵防御中の斬を遅い防御崩しへ変換し、青輪が消えた場合は空振りして隙を残すようにした。
- 通常斬撃が敵防御へ衝突した場合は弾かれ、反撃受付を与えず、敵の次攻撃を早めるようにした。
- プレイヤー構え値を追加。通常防御で減少し、0で防御崩れと短い入力不能時間が発生する。
- 重量型とボス後半は通常防御への構え圧力を増やした。
- 受け流しはダメージ0、構え回復、敵構えへの大きなダメージ、反撃受付、高得点を与える。
- 敵構え値を追加。構えダメージは `通常斬撃 < 反撃斬り < 防御崩し` とした。
- 敵構え0で大崩れ時間を作り、その間の斬を強撃へ変換した。
- 正方向の安全時間内回避だけに得点と構え回復を与えた。

### 通常敵7種

1. 影面: 左固定の左右教材型
2. 角岩: 右固定の左右教材型
3. 鳴壺: 左右交互攻撃型
4. 逆鉾: 右→右→左の三手型
5. 霧猿: 予備前半だけ追尾し、後半で狙いを固定する追尾型
6. 鉄輪: 一撃目の反対側へ二撃目を出す二段攻撃型
7. 骨灯: 遅いが高威力・高構え圧力の重量型

### ボス・章構成

- 第1章は獣型、第2章はモンスター型、第3章は人型、第4章は鳥型、第5章はモニュメント型の既存ボス2体ずつを出す。
- 同じ候補を直後に再選択しない。
- 通常敵も章別プールにし、固定左右 → 型／防御 → 追尾／フェイント → 二段／重量 → 全組み合わせの順で解禁する。
- ボス体力50%以下で第二段階へ移行し、短い崩れの後に前半の攻撃を組み合わせる。
- 獣型は突進の連撃、モンスター型は拡大範囲と後半の追撃、人型は一度だけの可視フェイントと後半の防御、鳥型は移動先からの急降下と後半の追撃、モニュメント型は不動の順次地面攻撃とした。
- 予告方向が変化した際は、地面・武器・HUD方向を同時に更新する。

### 関連修正

- スマホ再挑戦／一時停止画面の「最初から」を安全な無引数コールバックへ修正した。
- リタイア／敗北時に保留中の斬撃、防御崩し、連続攻撃を破棄した。
- 一時停止開始時点ですでに終了していた期限を復帰時に再有効化しないようにした。
- 体力とは別の構えメーター、敵大崩れ、ボス段階、斬撃段階をHUDへ追加した。

## 4. 影響範囲

- `client/src/game/rules.ts`: 戦闘数値、敵規則、章プール、構え、入力ロックの純粋関数
- `client/src/game/scene.ts`: 実戦闘、敵生成、ボス段階、入力、停止復帰、3D予告
- `client/src/App.tsx`: 構え／段階表示と再挑戦呼び出し
- `client/src/index.css`: 構えメーターと大崩れ表示
- `client/src/game/rules.test.ts`: 優先度S+Aの自動テスト

## 5. 確認結果

- Vitest: 23件成功
- TypeScript構文診断: 0件
- TypeScript厳格セマンティック診断: 0件
- 斬連打: 攻撃全体の期限中は再入力不可
- 防御連打: 防御受付中の再入力不可。通常防御を続けると構えが尽きる
- 防御＋回避同時押し: 相互排他で重複防御不可
- 左右回避連打: 安全時間は回避全体の一部だけ。正方向だけ加点
- 敵防御への通常斬撃: 反撃受付なし、プレイヤーに弾かれ隙
- 受け流し: プレイヤー受け流し時だけ反撃受付
- アプリ中断・復帰: 有効期限だけを延長し、終了済み受付は復活しない
- リタイア／敗北: 保留攻撃で結果が後から変化しない

## 6. 残課題

- 実機iPhone Safariでの長時間バランス確認と、構えメーターを含む狭幅HUDの最終目視確認。
- 優先度Bの10／25／50体モード、章報酬3択、難易度、結果統計、自己最高記録、同一シード再挑戦。
- 優先度Cの左利き配置、音量分離、描画負荷自動調整、戦闘コードの段階的分割。
