# 優先度S 実装記録

## 基準コード

- 対象リポジトリ：`chameleonjp-lab/mamonokiri`
- 再調査した `main`：`77c4247b48773f0e81d2fcca5a317c9a5cc6789a`
- 作業ブランチ：`codex/priority-s-combat-fixes-20260820`
- 参照資料：`実装組織図.txt`（実装・検査を Luna / Max、重大判断を Sol が担当する分担）

## 作業開始時の優先度S判定

| 項目 | mainの状態 | 原因・関連問題 |
|---|---|---|
| 敵攻撃回数 | 未修正 | 攻撃開始時に加算した `enemyAttackCount` を攻撃終了時に `0` へ戻していた。交互・指定回数パターンが成立しない。 |
| ボス攻撃方向 | 未修正 | `boss` だけで両側攻撃扱いになり、設定の左右と危険範囲・槍の表示・命中判定が一致していなかった。 |
| 再挑戦初期化 | 未修正 | Rキーのインライン処理がスコア、連撃、各タイマー、停止・遷移状態などを完全には初期化していなかった。 |
| スマホ再挑戦 | 未修正 | 結果画面とタッチ再挑戦ボタンがなく、Rキー依存だった。 |
| 章クリア判定 | 未修正 | 次の敵を生成する処理で、10体目などの出現時に回復・報酬が発生していた。 |
| 最初の3体 | 未修正 | 初期敵も通常のランダム選択で、左右回避→逆方向→防御・受け流しの順序が固定されていなかった。 |
| 敵防御処理 | 未修正 | 敵のガードへ通常斬撃を当てるとプレイヤーに `counterUntil` を与えていた。敵に防がれただけで反撃権が発生する。 |
| 10連判定 | 未修正 | 敵撃破時の `combo === 10` に依存し、10以上へ到達した瞬間を扱っていなかった。20連以降も同様。 |
| 一時停止・復帰 | 未修正 | pauseフラグだけで、Safariのバックグラウンド復帰、絶対時刻タイマーの補正、常設停止UIがなかった。 |
| 敵切替中の停止 | 未修正 | 撃破後の次敵待ち時間に、戦闘入力・敵攻撃・連撃時間が明確に停止されていなかった。 |

## 実装順

1. 戦闘ルールを `client/src/game/rules.ts` へ抽出し、攻撃方向、チュートリアル、章報酬、連閃、遷移、停止補正を決定論的にテストできるようにする。
2. `scene.ts` の共通リセットと撃破遷移を先に直し、攻撃回数を敵の生存期間中維持する。
3. ボスを含む攻撃計画から、武器表示・危険レーン・命中判定を同じ値で駆動する。
4. 敵ガード、受け流し、反撃受付、コンボ閾値を分離する。
5. React側に結果画面、停止画面、タイトル復帰、リタイア、スマホ操作を追加する。
6. 可視性変更時の自動停止と、復帰時のタイマー補正を追加する。

## 変更内容

### 戦闘・進行

- 攻撃開始時だけ `enemyAttackCount` を `+1` し、攻撃終了時にはリセットしない。敵出現時と共通リセット時だけ `0` に戻す。
- `attackPlanFor` が左右・右左交互・両側・追尾を決定し、槍の表示、警告、危険レーン、命中判定へ同じ計画を渡す。
- 最初の3体を固定し、1体目は左攻撃、2体目は右攻撃、3体目は長い予告付きの両側攻撃で防御・受け流しを促す。チュートリアル中は開始位置も危険側へ置く。
- 敵ガード中の通常斬撃は弾き返しとして処理し、`counterUntil` を消す。敵ガード中の斬撃だけは防御崩し処理へ入り、敵攻撃の受け流し成功だけが反撃受付を作る。
- 連閃は `9→10`、`19→20` のような閾値通過で一度だけ演出値を増やす。
- 敵撃破後は入力・敵攻撃・連閃時間を停止し、次の敵を生成してから戦闘を再開する。50体目の撃破だけ結果画面へ移る。
- 回避の安全時間を移動全体ではなく中間区間へ限定し、正しい方向の回避だけを成功扱いにする。防御連打は同じ防御受付を更新できない。

### 初期化・端末操作

- `resetRun` を唯一の再挑戦初期化処理として、Rキー、結果画面、停止画面、タイトル画面から共通利用する。
- HP、敵HP、敵番号、ボス、スコア、連閃、最大連閃、反撃、攻撃・防御・回避・敵攻撃・斬撃・演出タイマー、停止・勝敗・敵切替状態、敵攻撃回数、表示位置を初期化する。
- 結果画面に「もう一度遊ぶ」「最初の画面へ戻る」を追加し、停止画面に「再開」「リタイア」「最初から」「タイトルへ」を追加する。
- `visibilitychange` でSafariのバックグラウンド遷移を自動停止する。復帰時に、停止中に経過した時間を攻撃・防御・回避・反撃・連閃・敵攻撃などの期限へ加算する。
- タッチUIは左・右・斬・防の4操作を維持し、safe-area、縦横画面、狭い幅向けの既存CSSと一時停止UIを併用する。

## 検証結果

- `client/src/game/rules.test.ts`：14 tests passed。
- App、GameCanvas、scene、rules、rules.test のTypeScript構文トランスパイル：全ファイルOK。
- 静的敵対検証：攻撃回数リセットは敵出現時・共通リセット時のみ、反撃受付の付与は受け流し分岐のみ、R/スマホ/タイトルの再挑戦イベントは同じ `resetRun` へ接続。
- 連打対策：攻撃受付中のJ、受付中のK、回避受付中の左右入力、repeat状態のRを拒否する入口を確認。

## 残課題

- この作業単位では優先度Sを中心に修正した。構え値、敵構え値、防御崩しの発生差、通常敵7種類の固有パターン、ボス後半戦、章別モード、3択報酬、難易度選択、詳細結果統計、自己ベスト、固定シード、描画負荷計測は次の作業単位に残す。
- 結果画面は現時点で到達敵数・撃破数・スコア・最大連閃を表示する。受け流し数、正しい回避数、被弾数、プレイ時間、自己最高記録は次段階で追加する。
- 実機iPhoneのSafariでの縦横・ノッチ・画面バー・負荷確認、依存関係を含む `pnpm check` / `pnpm build`、GitHub Pages公開環境での最終確認は、ブランチ上のブラウザ検証工程で実施する。
