# 敵対的検証レポート

## 検証範囲

現行のBabylon.jsシーンとReact HUDを対象に、連打・高速入力、回避境界、敵攻撃状態、撃破後の再起、コンボとスコア、戻る操作、長押し抑制、スマホ表示をコードと実行時ログの両面から点検した。型検査と本番ビルドは成功し、直近のデスクトップおよび390×844表示では3Dシーンが描画され、ブラウザ起動直後の新規エラーは確認されなかった。

## 発見事項

| 重大度 | 発見 | 再現条件 | 影響 |
|---|---|---|---|
| 高 | 攻撃入力に実質的なクールダウンがない | `J`を約360ms以内に複数回発火する、または高速に連打する | `attackUntil`は見た目の状態に使われるだけで、攻撃判定を拒否しない。敵HPを短時間で削り切れるため、剣戟のタイミングと難易度を回避できる |
| 高 | 一時停止中もキーボード入力が状態を変更できる | 戻る確認ダイアログ表示中に`J/K/Shift/R`を送る | レンダーループは停止するが、`keydown`は`paused`を確認しない。ダイアログ中に攻撃、回避、防御、再起が進みうる |
| 中 | 戻る確認はタッチ端末に限定される | デスクトップブラウザで戻る操作を行う | `touchCapable`が偽の場合、履歴ガードと確認ダイアログが登録されず、ユーザーの意図確認なしにページを離れる可能性がある |
| 中 | 攻撃予告と攻撃状態がHUDの構造化状態にない | 敵攻撃中の表示を外部UIや自動テストで判定する | `message`だけで攻撃状態を表現しており、攻撃中・命中判定区間・復帰区間をUIが明示的に扱えない |
| 低 | スマホの「避」ボタンは常に右方向イベント | 左右専用ボタンを使わず避ボタンを押す | スワイプは方向を保持するが、タッチボタンは右回避だけを発火する。左右の手動選択が必要な端末操作では表現力が不足する |

## 防御できている点

プレイヤーの左右位置はレンダー幅と高さから算出した`sideLimit()`でクランプされるため、通常の回避入力では画面外へ出にくい。左右スワイプは距離・時間・横方向優位性を条件にしており、短いタップや縦スクロール様の入力による誤回避を抑制している。敵攻撃の命中判定は攻撃の一回の振り抜き区間に限定され、回避中または防御中はダメージを受けない。

## 修正優先度

最優先は攻撃クールダウンの実判定化である。`J`処理の冒頭で`attackUntil > now`中の再攻撃を拒否し、攻撃が完了してから次の判定を許可するべきである。次に、`keydown`とカスタム回避イベントの入口で`paused`を確認し、確認ダイアログ中の状態変更を遮断するべきである。戻る操作の要件を一貫させるなら、タッチ端末だけでなくデスクトップでも履歴ガードと確認ダイアログを登録する必要がある。

## 総合評価

現状は、表示・基本操作・敵攻撃演出・左右境界の基盤は成立している。一方、**高速攻撃入力による難易度バイパス**と**一時停止中の入力貫通**は、プレイヤーが意図せず、または自動入力によって容易にゲーム状態を壊せるため、公開前に修正すべき高優先度の問題である。
