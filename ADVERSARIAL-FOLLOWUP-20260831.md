# マージ後の敵対的検証と後続対応

対象は `main` の `30a31b72908992fee112fe5db8ab1b1527584f5d`（PR #15 マージ後）です。

## 検証で確認した問題

| 重要度 | 確認内容                                                                                                         | 影響                                                                            |
| ------ | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| 高     | `scene.ts` の `state()` は攻撃・反撃受付・連撃時間・経過時間を計算するが、状態通知は主にイベント発生時だけだった | 攻撃中にHUDの受付表示、連撃ゲージ、敵の攻撃フェーズ、プレイ時間が画面上で止まる |
| 高     | `vite-plugin-manus-runtime` が本番HTMLへランタイムを注入していた                                                 | GitHub PagesのHTMLが約367KBとなり、ゲームに不要なエディタ用コードを公開する     |
| 中     | 固定構図のゲームカメラに `attachControl` が設定されていた                                                        | スワイプやキーボード操作がゲーム入力と競合し、意図せずカメラを動かせる          |
| 中     | PR向けのActions検査がなく、検査は `main` のPagesデプロイ時だけだった                                             | マージ前にテスト・型検査・Pages用ビルド結果を確認できない                       |

## 後続対応

- 戦闘中だけHUD状態を100ms周期で通知し、時間依存の表示を追従させる。
- 固定カメラへの入力接続を削除し、入力を斬・防・避けに限定する。
- Manusのランタイム、デバッグ収集、ストレージプロキシ、JSX位置情報計装、未使用ダイアログと依存パッケージを削除する。
- `pull_request` 用の軽量な検査Workflowを追加し、`test`、`check`、Pages用ビルド、公開アーティファクトへのエディタコード混入検査を実行する。
- 画像・テクスチャは実行時に読み込まず、既存WebPは造形基準画として保管する方針を文書化する。

## 未実施の実機確認

- iPhone 17 ProのSafariでの縦画面、Dynamic Island付近、Safariバー、長時間発熱、フレーム落ち、同時タッチは実機が必要なため未確認。
- この実行環境ではブラウザ自動操作のソケット制限により、ローカルの視覚確認は完了できない。型検査・テスト・本番パス付きビルドと、既存PagesへのHTTP到達確認は別途実施済み。
