# Priority B 実装記録

作業ブランチ: `codex/priority-b-modes-records-20260820`

基準main: `70d507d61e268dd837c0d0eb92087023142b2c42`

## 作業開始時の確認

- PR #3（Priority A）は `merged: true` でmainへ反映済み。
- 攻撃回数の維持、最初の3体、敵構え値、受け流し、攻撃後の隙、ボス後半戦、停止処理は既存実装を引き継いだ。
- Priority Bの未実装項目は、10/25/50体モード、難易度選択、章3択報酬、詳細結果統計、自己ベスト、固定シード、スコア上限だった。
- 章報酬は撃破処理内で自動回復・自動加点されており、選択の余地がなかった。

## 原因と変更

### モードと難易度

- `RunMode` を `ten` / `twenty-five` / `fifty` に分け、残敵数・勝利判定・結果画面をモード上限から計算するようにした。
- タイトル画面で「十番勝負」「二十五番勝負」「五十番修行」を選べるようにした。
- `Difficulty` を `apprentice` / `standard` / `dark` に分け、予告時間、敵の間合い、受け流し受付時間を明示的に変えるようにした。

### 章報酬

- 10体、20体などを撃破した後、次の敵を出す前に戦闘を停止し、3択を表示するようにした。
- 「生命を整える」「静かな見切り」「修験の勢い」から1つを選ぶ。
- 報酬選択中は攻撃、入力、コンボ期限、タイマーを進めない。
- 最終体の撃破は次の章がないため、報酬選択を出さず結果画面へ移る。

### スコアと結果記録

- コンボ倍率を8倍で上限化した。
- 受け流し、正しい回避、無傷撃破、空振りなしを個別加点し、通常命中だけの連打依存を弱めた。
- 結果画面にモード、難易度、撃破数、ボス撃破数、受け流し成功数、正しい回避数、被弾数、空振り数、プレイ時間、敵順コードを表示する。
- 自己ベストは `モード × 難易度` ごとに端末内へ保存する。

### 再現可能な敵順

- 走行開始時に32-bitシードを生成し、結果画面から同じシードで再挑戦できるようにした。
- 敵選択用乱数と戦闘演出用乱数を分離し、プレイ中の移動やガード発生回数が敵順に影響しないようにした。

## 影響範囲

- `client/src/game/rules.ts`: モード、難易度、報酬、シード、スコアの純粋なルールを追加。
- `client/src/game/scene.ts`: 走行設定、報酬停止、戦績収集、自己ベスト、再現乱数を追加。
- `client/src/App.tsx`: タイトル選択、章報酬、結果統計、同じ敵順再挑戦を追加。
- `client/src/index.css`: iPhone縦画面でも選択肢と結果が収まるUIを追加。
- `client/src/game/rules.test.ts`: Priority Bの純粋ルールテストを追加。

## 確認結果

- Vitest: **31件成功**
- TypeScript strict: **0件**
- scene / App のesbuild構文・バンドル検査: **成功**
- 章報酬: 9体撃破時は0件、10体撃破時は3件、10体モードの最終撃破時は0件をテスト。
- スコア: 80連でもコンボ倍率が8倍を超えないことをテスト。
- シード: 同じシードから同じ乱数列を再生成することをテスト。

## 敵対的検証

- 斬連打: 斬撃の発生・命中・振り終わりを既存の入力ロックで維持し、敵防御時は反撃権を付与しない。
- 防御連打: 構え値と既存の構え崩れを維持し、報酬選択中は入力自体を停止する。
- 左右回避連打: 回避安全時間と正方向加点を維持し、同方向連打を新しい抜け道にしない。
- 画面外タップ: 既存のゲームシェル／ボタン以外のタップを戦闘入力へ変換しない。
- 高速連打: `canStartPlayerAction` とイベントの報酬選択検証で重複処理を抑止する。
- アプリ中断・復帰: 既存の停止時タイマー補正に加え、プレイ時間と章報酬選択を停止対象にした。

## 残課題

- iPhone SE相当、横画面、Dynamic Island、Safari上下バーを含む実機目視は未実施。
- 実機でタイトル選択・章報酬カード・結果統計の縦横レイアウトとタップ領域を確認してからReady化する。
- Priority Cの左利き配置、音量分離、描画負荷の段階制御、コード分割、自動テストのシーン統合検査は未着手。
