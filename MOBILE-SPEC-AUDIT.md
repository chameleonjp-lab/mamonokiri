# スマホゲーム仕様・実装差分監査

基準コード: `main` / `06dc0cbcc564768e3e960436fa1396caf414b9b2`  
監査日: 2026-08-23

## 結論

戦闘の主要機能は実装済みでしたが、スマホ実機で遊ぶ前提では、入力、案内文、復帰安全性、狭い画面の配置、設定画面のスクロール、描画品質の計算に仕様との差がありました。本変更では、ボタンを増やさず「左・右・斬・防」の4操作を維持したまま、プレイ不能または誤判断につながる差を優先して修正します。

## 確認した差と対応

| 項目         | mainで確認した事実                                                      | 本来あるべき状態                                             | 対応                                                              |
| ------------ | ----------------------------------------------------------------------- | ------------------------------------------------------------ | ----------------------------------------------------------------- |
| タッチ入力   | 斬・防ボタンがJ/Kのキーボードイベントを擬似発火                         | タッチとキーボードが同じ戦闘処理を直接呼ぶ                   | 斬・防のゲームイベントを追加し、入力経路を共通化                  |
| 序盤3体      | 敵順は固定だが、課題成功前でも斬って進行可能                            | 1・2体目は正しい回避、3体目は受け流し成功後に進行            | 成功フラグを追加し、成功前の斬を受け付けない                      |
| 操作案内     | スマホ画面にもJ/K、GUARD、BREAKを重要表示                               | 重要判断は日本語のボタン名と状態名で表示                     | 「斬」「防」「防御」「大崩れ」「前半・後半」に統一                |
| Safari中断   | `visibilitychange`だけで停止し、復帰直後の猶予なし                      | タブ移動・アプリ中断で停止し、手動復帰後に安全時間を置く     | `pagehide`を追加し、復帰時に700msの猶予を全戦闘タイマーへ反映     |
| 情報画面     | 全要素が`touch-action:none`で、結果・設定画面も指でスクロール不能       | 戦闘面は固定、タイトル・設定・結果は縦スクロールと拡大が可能 | オーバーレイ表示中だけ`pan-y pinch-zoom`を許可                    |
| ブラウザ機能 | コピー、選択、長押しを文書全体でJavaScript禁止                          | ゲーム操作だけ誤動作を防ぎ、情報画面のアクセシビリティは残す | 文書全体のイベント禁止を削除し、CSSの適用範囲を限定               |
| Viewport     | `lang=en`、Safe Area用の`viewport-fit=cover`なし、拡大上限あり          | 日本語文書、ノッチ対応、情報画面で拡大可能                   | `lang=ja`、動的viewport、Safe Area、テーマ色を設定                |
| 縦画面HUD    | 一時停止が敵HUDと重なり、320px幅では操作ボタンの最小幅が収まらない      | 戦況、停止、4ボタンが互いを隠さない                          | HUD中央に停止領域を確保し、ボタンを可変2列に変更                  |
| 横画面HUD    | 高さ判定では操作ボタンだけ出し、ブランド・停止・構えがHUDやボタンと競合 | 左に移動、右に斬・防、中央に得点・停止・メッセージ           | 高さ500px以下の横画面レイアウトを全面定義                         |
| 開始前設定   | 左利き・音・描画設定は開始後の一時停止画面だけ                          | 最初の戦闘前に端末設定を選べる                               | タイトルへ操作配置、全消音、各音量、演出、描画設定を追加          |
| 描画品質     | Babylon.jsの倍率解釈と逆で、高精細ほど低解像度                          | 高精細 > 標準 > 軽量の順で内部解像度を下げる                 | 目標ピクセル比の逆数に修正し、DPRを2までに制限                    |
| 待機中描画   | タイトル・一時停止・結果画面の背後でも3Dを毎フレーム描画                | 戦闘停止中は必要な静止画だけを残し、GPU負荷を止める          | 停止状態では描画ループを止め、再開時に同じループを再開            |
| ボス回復     | 5体ごとの全ボス撃破で自動的に体力・構えを30回復                         | 回復は10体撃破後の3択報酬として選んだ場合だけ                | 自動回復を削除し、章報酬の回復だけを有効化                        |
| GitHub Pages | workflowがなく、リポジトリ配下パス用の公開手順もない                    | mainへ承認マージ後、自動テストを通して公開                   | テスト・型検査・`/mamonokiri/`向けビルド・Pages配信workflowを追加 |

## 敵対的検証

- 斬連打: 攻撃の発生・命中・振り終わり中は次の入力を拒否。チュートリアル成功前は進行不可。
- 防御連打: 行動中入力を拒否し、通常防御では構え値が減る既存仕様を維持。
- 左右連打: 回避行動中は次の回避を拒否し、安全時間は回避全体の一部だけ。
- 高速タップ: タッチイベントは同じ`canStartPlayerAction`判定を通る。
- 画面外スワイプ: ボタン、停止、結果、報酬、敵切替中はスワイプ入力を無効化。
- 中断・復帰: 停止時間に加え700msを戦闘期限へ加算し、復帰直後の命中を防止。
- 報酬の抜け道: 5体ごとの自動回復を削除し、10体撃破後の選択と重複しない。

## 確認結果

- Vitest: 既存テストと追加したスマホ契約テストを実行。
- TypeScript: `tsc --noEmit`を実行。
- Production build: Viteの本番ビルドを実行。
- CSS: Vite/PostCSS変換を通し、320×568、390×844、568×320、844×390相当のルールを数値確認。

## 残る実機確認

この作業環境からiPhone Safariそのものは操作できないため、次の項目はDraft PRのプレビューまたはPages反映後に実機確認が必要です。

- Safari上下バーが展開・縮小した瞬間の見切れ
- Dynamic Island周辺の視覚的な余白
- 画面回転直後のCanvas再描画
- 長時間プレイ時の発熱とフレーム落ち
- 実際の指での同時押し、連打、スワイプ感度
- GitHub Pagesが未有効の場合は、リポジトリ設定で公開元を「GitHub Actions」にする初回設定

実機で差が出た場合も、戦闘ロジックを変えずCSSのSafe Area・寸法設定を調整できる構造です。
