# Priority C2 実装記録：設定共通化・入力キャンセル対策

## 確認した状態

- 対象：`main` にマージ済みの PR #5（merge commit `ae75b109c207f75940af4ede9bf03b28a57f17fe`）
- 対象端末：iPhone 17 Pro。特に縦画面、Safariの画面離脱、ピンチ・システムジェスチャーによるポインター中断を想定。
- 既存の戦闘ルール、ボタン配置、音の演出、描画段階（高精細／標準／軽量）は変更していない。

## 原因・確認結果

1. 音量・ミュート・左右配置・描画品質のlocalStorageキーが複数ファイルに直接書かれており、キー変更時にAppと3Dシーンの読み取りがずれる余地があった。
2. 描画品質のDPR計算がReactコンポーネント内にあり、端末DPRの上限・下限を単体テストで固定できなかった。
3. スワイプ回避入力に`pointercancel`処理がなく、ピンチ、画面端ジェスチャー、アプリ中断で開始座標が残ると、復帰後のpointerupが回避入力になる可能性があった。

## 変更内容

- `SETTINGS_STORAGE_KEYS` と `readAudioSettings` を追加し、App・GameCanvas・sceneの設定読み取り／保存を共通化。
- `hardwareScalingLevelFor` を追加し、DPRを1〜2に収めて描画品質ごとの内部解像度を決定。
- 音声設定の永続値、描画品質のDPR計算、既定値へのフォールバックをテスト対象へ追加。
- `pointercancel` でスワイプ状態を破棄。タイトル、ポーズ、結果、章報酬、敵切り替え中はスワイプ回避を受け付けない。

## 影響範囲

- 設定の保存形式（既存のキー名）は維持しているため、PR #5で保存済みの端末設定を引き継ぐ。
- 軽量設定でも危険範囲などの判断情報は変更せず、霧・粒子・内部解像度の既存方針を維持する。
- `pointercancel`後に復帰した最初のタップが、過去のスワイプ入力として誤発火しなくなる。

## 検証結果

- Vitest：2ファイル、37テスト全件成功。
- TypeScript：`tsc -p tsconfig.json --noEmit` 成功。
- esbuild：App、GameCanvas、sceneのブラウザ向けバンドル成功。
- 静的敵対チェック：直接記述された設定localStorageキーなし、`pointercancel`の登録あり、停止・結果・報酬・敵切り替え中のスワイプ遮断あり。
- 連打系入力について、今回の変更が通常の斬・防・左右ボタンのイベント経路へ介入していないことを確認。

## 残課題

- 実機Safariで縦／横回転、Dynamic Island周辺、画面上下バー表示中の視認性を確認する。
- 次段階で、設定画面の表示文言とゲーム内の入力受付状態をさらに整理する。
- フルVite buildは、この作業用コピーに公開構成の`index.html`／`server`が含まれていないため未実施。GitHub上の完全なmain checkoutまたはCIで確認する。
