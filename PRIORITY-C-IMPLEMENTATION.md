# Priority C 実装記録

## 対象

PR #4 のマージ後に取得した `main` を確認し、iPhone 17 Pro を最優先端末として、操作配置・音設定・描画負荷の残課題を実装した。

## 確認したこと

- `client/src/App.tsx`：スマホ操作は左右回避が左、斬・防が右で固定。左利き切替は未実装だった。
- `client/src/components/GameCanvas.tsx`：`preserveDrawingBuffer` と `adaptToDeviceRatio` が常時有効で、描画品質の切替がなかった。
- `client/src/game/scene.ts`：霧は常時有効。パーティクルシステムとポストエフェクトの実体は確認できなかった。
- 音設定は環境音量だけで、効果音・主音量・全消音が分離されていなかった。

## 原因

戦闘と表示の設定が `App.tsx` と `scene.ts` に個別に存在し、端末設定を共通の型・保存形式で扱う層がなかった。そのため、iPhoneのDPRや音の種類ごとの調整を実行時に切り替えられなかった。

## 変更内容

- `client/src/game/config.ts` を追加し、音量、片手配置、描画品質の設定と正規化処理を共通化。
- 左利き設定を端末内に保存し、スマホの移動面と斬・防面を左右反転。
- 主音量・効果音量・環境音量・全消音を個別に保存し、再生時にカテゴリ別へ反映。
- ゲーム開始時の既存AudioContext初期化を維持し、ユーザー操作前に音を鳴らさない構成を維持。
- 高精細／標準／軽量の描画設定を追加。内部描画解像度を調整し、軽量時は霧と粒子状の画面ノイズを省略する。
- 画面保存用バックバッファを常時確保しないように変更。現コードにスクリーンショット機能はないため、ゲーム判断への影響はない。
- `config.test.ts` を追加し、音量境界、設定値の正規化、描画品質の差を検証。

## 影響範囲

- 戦闘判定、敵生成、攻撃回数、コンボ、スコア、勝敗、再挑戦ルールは変更していない。
- 既存の左右回避・斬・防の論理入力は変更していない。
- 軽量設定でも危険範囲、敵の構え、攻撃予告、反撃受付の情報は削除しない。

## 確認結果

- Vitest：35テスト成功。
- TypeScript strict：エラーなし。
- `scene.ts` と `App.tsx` の esbuildバンドル：成功。
- scratchコピーには公開用 `index.html` と `server/` が含まれていないため、Viteのフルビルドはエントリ不足で実行できなかった。これは今回の変更によるエラーではない。

## 敵対的検証

- 斬連打：音量設定の追加だけで、攻撃受付や硬直の判定を変更していない。
- 防御連打：同上。設定操作は一時停止画面のボタンに限定。
- 左右回避連打：左右の論理方向は維持し、配置だけを反転。
- 高速設定変更：音量は0〜1へクランプし、未知の保存値は標準値へ戻す。
- アプリ中断・復帰：既存の自動一時停止処理を維持し、描画品質変更でタイマーを動かさない。

## 残課題

- 実機のiPhone 17 Proで、縦横回転、Dynamic Island、Safariの上下バーを含む表示確認が必要。
- 端末ごとの実測FPSを取得する計測画面は未追加。
- 章構成、敵・ボスの読み合い、スコア改善などPriority A/Bの次段階は別作業として継続する。
